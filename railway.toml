[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Run node directly - bypass npm start which triggers prestart rebuild
startCommand = "node dist/index.js"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

